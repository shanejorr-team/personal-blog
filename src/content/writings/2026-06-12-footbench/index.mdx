---
title: "Footbench"
description: "An LLM evaluation to measure intuition and confidence in beliefs."
date: 2026-06-12
tags: ["AI", "LLM", "evaluation"]
draft: true
---

import ModelOutputTables from '../../../components/footbench/ModelOutputTables.astro';
import StaticChart from '../../../components/footbench/StaticChart.astro';
import overallPairwiseResultsSvg from './charts/overall-pairwise-results.svg?raw';
import pairwiseResultsByCriterionSvg from './charts/pairwise-results-by-criterion.svg?raw';
import pairwiseResultsByModelSvg from './charts/pairwise-results-by-model.svg?raw';
import subjectivePriorsSvg from './charts/subjective-priors.svg?raw';
import interraterReliabilitySvg from './charts/interrater-reliability.svg?raw';
import orderConsistencySvg from './charts/order-consistency.svg?raw';

## What is Footbench?

Evaluates three things:
- reasoning with evidence
- reasoning on intuition
- measuring confidence in beliefs

Evaluated through pairwise competition
- each model (candidate models) competes against all other models
- compete twice, changing the order each time
- four models judge which candidate model did best in each head to head matchup
- Criteria to pick winner:
    1. **Soundness of recommendations**
        - Better NFL strategy picks: underutilized, credibly win-positive, football-literate
        rationales, correct analytics-vs-intuition bucket, distinct picks in the right grid cells.
    2. **Reasonableness of Bayesian priors**
        - Better prior choices: distribution family matches shape/tails/support, plausible
        football effect sizes, reasonable 95% intervals, correct conversions, honest uncertainty
        for weak evidence cells.
    - Final displayed score is win percentage: wins / comparisons * 100, overall or by criterion.

## Results

### Competition results

<StaticChart
  svg={overallPairwiseResultsSvg}
  title="Overall pairwise win percentage"
  maxWidth={760}
/>

<StaticChart
  svg={pairwiseResultsByCriterionSvg}
  title="Pairwise win percentage by criterion"
  maxWidth={820}
/>

<StaticChart
  svg={pairwiseResultsByModelSvg}
  title="Candidate win percentage by judge model"
  maxWidth={920}
/>

### Comparing priors

<StaticChart
  svg={subjectivePriorsSvg}
  title="Subjective prior distributions by model"
  maxWidth={880}
/>

## Model Output

<ModelOutputTables />

## Judge scoring consistency

<StaticChart
  svg={interraterReliabilitySvg}
  title="Four-judge vote splits by criterion"
  maxWidth={720}
/>

<StaticChart
  svg={orderConsistencySvg}
  title="Within-judge order consistency"
  maxWidth={760}
/>
