# Face detection and image processing dependencies
face_recognition>=1.3.0
opencv-python>=4.5.0
numpy>=1.20.0

# Note: face_recognition requires dlib, which requires CMake
# Install CMake first: brew install cmake (macOS) or apt-get install cmake (Linux)
